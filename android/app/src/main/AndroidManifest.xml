<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Suppress warning, just using queries tag is not enough for our all, we need access to all
    packages to allow the user to select apps for split tunneling -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- https://developer.android.com/guide/components/fg-service-types#system-exempted -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Added by work manager, it is not required for our use case. -->
    <uses-permission
        android:name="android.permission.WAKE_LOCK"
        tools:node="remove" />
    <uses-feature android:name="android.hardware.touchscreen"
                  android:required="false" />
    <uses-feature android:name="android.hardware.faketouch"
                  android:required="false" />
    <uses-feature android:name="android.hardware.screen.portrait"
                  android:required="false" />
    <uses-feature android:name="android.hardware.screen.landscape"
                  android:required="false" />
    <uses-feature android:name="android.software.leanback"
                  android:required="false" />
    <uses-feature android:glEsVersion="0x00020000"
                  android:required="false" />

    <application android:name=".app.MullvadApplication"
                 android:allowBackup="false"
                 tools:targetApi="33"
                 android:memtagMode="async"
                 android:fullBackupContent="@xml/full_backup_content"
                 android:dataExtractionRules="@xml/data_extraction_rules"
                 android:icon="@mipmap/ic_launcher"
                 android:label="@string/app_name"
                 android:roundIcon="@mipmap/ic_launcher"
                 android:theme="@style/Theme.App.Starting"
                 android:localeConfig="@xml/locales_config"
                 android:supportsRtl="true"
                 tools:ignore="CredManMissingDal,CredentialDependency,GoogleAppIndexingWarning">

        <!--
            This disables the Firebase logging that is added by the billing library.
        -->
        <service
                android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService"
                tools:node="remove"
                tools:ignore="MissingClass" />

        <profileable
                android:enabled="true"
                android:shell="true" />

        <!--
            MainActivity
            Must be exported in order to be launchable.
            Launch mode should singleInstance to avoid this vulnerability;
            https://developer.android.com/privacy-and-security/risks/strandhogg
            This can be disregarded when the minimum supported version is 29 or higher
            since after that it has been patched on a OS level.
        -->
        <activity
            android:name="vu.vpn.app.MainActivity"
            android:enableOnBackInvokedCallback="true"
            android:launchMode="singleInstance"
            android:screenOrientation="fullUser"
            android:windowSoftInputMode="adjustResize"
            android:enabled="true"
            android:exported="false"
            android:icon="@mipmap/ic_launcher"
            android:banner="@mipmap/ic_banner"
            android:targetActivity="vu.vpn.ui.MainActivity"
            tools:ignore="DiscouragedApi,UnusedAttribute">

            <intent-filter>
                <action android:name="vu.vpn.request_vpn_permission" />
            </intent-filter>
        </activity>

        <!--
            Deep link "Abrir o app" pós-pagamento (vpnvu://app). Alias exported
            pro navegador conseguir abrir o app a partir da tela de sucesso do
            site. Aponta pra MainActivity, que no handleIntent dispara um refresh
            IMEDIATO do estado da conta (não espera o polling de 15s).
        -->
        <activity-alias
            android:name="vu.vpn.app.PaymentReturnAlias"
            android:exported="true"
            android:targetActivity="vu.vpn.app.MainActivity"
            tools:ignore="UnusedAttribute">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vpnvu" android:host="app" />
            </intent-filter>
        </activity-alias>
        <!--
            foregroundServiceType="systemExempted" is required in Android 14+
            https://developer.android.com/guide/components/fg-service-types#system-exempted
            -->
        <service android:name="vu.vpn.app.service.MullvadVpnService"
                 android:exported="false"
                 android:foregroundServiceType="systemExempted"
                 android:permission="android.permission.BIND_VPN_SERVICE"
                 android:stopWithTask="false"
                 tools:ignore="ForegroundServicePermission">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <intent-filter>
                <action android:name="vu.vpn.connect_action" />
            </intent-filter>
            <intent-filter>
                <action android:name="vu.vpn.reconnect_action" />
            </intent-filter>
            <intent-filter>
                <action android:name="vu.vpn.disconnect_action" />
            </intent-filter>
            <intent-filter>
                <action android:name="vu.vpn.quit_action" />
            </intent-filter>
        </service>
        <!--
            MullvadTileService
            Tile services must be exported and protected by the bind tile permission
            (android.permission.BIND_QUICK_SETTINGS_TILE).
        -->
        <service android:name="vu.vpn.app.tile.MullvadTileService"
                 android:exported="true"
                 android:icon="@drawable/small_logo_black"
                 android:label="@string/toggle_vpn"
                 android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <provider android:name="vu.vpn.lib.common.provider.MullvadFileProvider"
                  android:authorities="${applicationId}.FileProvider"
                  android:exported="false"
                  android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                       android:resource="@xml/provider_paths" />
        </provider>
        <receiver android:name=".receiver.LocaleChangedBroadcastReceiver"
                  android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>
        <receiver
                android:name=".lib.pushnotification.receiver.NotificationAlarmReceiver"
                android:exported="false" />
        <receiver
                android:name=".receiver.AutoStartVpnBootCompletedReceiver"
                android:enabled="false"
                android:exported="false">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
                android:name=".receiver.ScheduleNotificationBootCompletedReceiver"
                android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!--
         Receiver to warn Android 16 users about VPN upgrade issue
         https://issuetracker.google.com/issues/441315112
         -->
        <receiver
            android:name=".receiver.Android16UpdateWarningReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
