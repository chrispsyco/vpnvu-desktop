<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- PSYCO · system splash com ícone transparente · só mostra o background
         deep navy igual o Compose splash · transição invisível pro nosso splash
         que aparece com o globo + logo + animações. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/AppTheme</item>
        <item name="windowSplashScreenAnimationDuration">1</item>
        <item name="windowSplashScreenBackground">@color/splashBg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_transparent</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/splashBg</item>
    </style>
</resources>
